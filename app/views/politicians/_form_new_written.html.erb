<%= form_for(@politician.statements) do |f| %>
  <% if @politician.statements.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@politician.statements.errors.count, "error") %> prohibited this written_statement from being saved:</h2>

      <ul>
      <% @politician.statements.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :issue %><br>
    <%= f.text_field :statements %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :source %><br>
    <%= f.text_field :path %>
  </div>
  <div class="field">
    <%= f.label :politician %><br> 
    <!-- will need to associate id with pol name for submission-->
    <%= f.text_field :politician_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
