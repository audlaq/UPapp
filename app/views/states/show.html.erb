<p id="notice"><%= notice %></p>

<p>
  <strong>State:</strong>
  <%= @state.state %>
</p>

    <% @state.politicians.each do |politician| %>
   <%= link_to politician.name, politician_path(politician) %><br>
    <% end %>

<br>
<%= link_to 'Back', states_path %>
